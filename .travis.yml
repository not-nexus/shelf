language: python
python:
    - 2.7
    - 2.6.6
install:
    - source utils/init-environment
    - pip install codecov
services:
    - elasticsearch
# This is required for elasticsearch
before_script:
    - sleep 10
script: coverage run --source pyshelf/ unit2 discover -p *_test.py tests -b
after_success:
    - codecov
